import { Any, AnyObject, Options } from "../../../types";
/**
 * Converts a document to an array of documents representing key-value pairs.
 */
export declare const $objectToArray: (obj: AnyObject, expr: Any, options: Options) => Any;
