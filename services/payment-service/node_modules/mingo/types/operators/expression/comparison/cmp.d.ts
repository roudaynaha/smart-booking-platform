import { Any, AnyObject, Options } from "../../../types";
/**
 * Compares two values and returns the result of the comparison as an integer.
 */
export declare const $cmp: (obj: AnyObject, expr: Any, options: Options) => Any;
