import { Any, AnyObject, Options } from "../../../types";
/**
 * Matches values that are less than the value specified in the query.
 */
export declare const $lt: (obj: AnyObject, expr: Any, options: Options) => boolean;
