import { Any, AnyObject, Options } from "../../../types";
/**
 * Computes the product of an array of numbers.
 */
export declare const $multiply: (obj: AnyObject, expr: Any, options: Options) => number;
