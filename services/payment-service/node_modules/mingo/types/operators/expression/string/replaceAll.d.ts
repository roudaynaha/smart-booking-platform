import { Any, AnyObject, Options } from "../../../types";
/**
 * Replaces all instances of a matched string in a given input.
 */
export declare const $replaceAll: (obj: AnyObject, expr: Any, options: Options) => Any;
