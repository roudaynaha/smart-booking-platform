import { Any, AnyObject, Options } from "../../../types";
/**
 * Evaluates an expression and returns the first non-null value.
 */
export declare const $ifNull: (obj: AnyObject, expr: Any[], options: Options) => Any;
