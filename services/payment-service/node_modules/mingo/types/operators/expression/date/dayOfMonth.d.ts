import { Any, AnyObject, Options } from "../../../types";
/**
 * Returns the day of the month for a date as a number between 1 and 31.
 */
export declare const $dayOfMonth: (obj: AnyObject, expr: Any, options: Options) => number;
