import { Any, AnyObject, Options } from "../../../types";
/**
 * Returns the minute for a date as a number between 0 and 59.
 */
export declare const $minute: (obj: AnyObject, expr: Any, options: Options) => number;
