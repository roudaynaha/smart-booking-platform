import { Any, AnyObject, Options } from "../../../types";
/**
 * Returns the seconds for a date as a number between 0 and 60 (leap seconds).
 */
export declare const $second: (obj: AnyObject, expr: Any, options: Options) => number;
