import { Any, AnyObject, Options } from "../../../types";
/** Returns the inverse sin (arc sine) of a value in radians. */
export declare const $asin: (obj: AnyObject, expr: Any, options: Options) => Any;
