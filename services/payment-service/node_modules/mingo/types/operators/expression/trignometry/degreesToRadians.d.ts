import { Any, AnyObject, Options } from "../../../types";
/** Converts a value from degrees to radians. */
export declare const $degreesToRadians: (obj: AnyObject, expr: Any, options: Options) => Any;
