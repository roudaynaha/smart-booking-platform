import { Any, AnyObject, Options } from "../../../types";
/** Returns the inverse cosine (arc cosine) of a value in radians. */
export declare const $acos: (obj: AnyObject, expr: Any, options: Options) => Any;
