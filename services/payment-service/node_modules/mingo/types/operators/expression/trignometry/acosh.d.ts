import { Any, AnyObject, Options } from "../../../types";
/** Returns the inverse hyperbolic cosine (hyperbolic arc cosine) of a value in radians. */
export declare const $acosh: (obj: AnyObject, expr: Any, options: Options) => Any;
