import { Any, AnyObject, Options } from "../../../types";
/** Returns the cosine of a value that is measured in radians. */
export declare const $cos: (obj: AnyObject, expr: Any, options: Options) => Any;
