import { Any, AnyObject, Options } from "../../../types";
/**
 * Concatenates arrays to return the concatenated array.
 */
export declare const $concatArrays: (obj: AnyObject, expr: Any, options: Options) => Any;
