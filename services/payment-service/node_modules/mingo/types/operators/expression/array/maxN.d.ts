import { Any, AnyObject, Options } from "../../../types";
interface InputExpr {
    n: Any;
    input: Any;
}
/**
 * Returns the n largest values in an array.
 */
export declare const $maxN: (obj: AnyObject, expr: InputExpr, options: Options) => Any;
export {};
