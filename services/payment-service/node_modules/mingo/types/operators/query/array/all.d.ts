import { Any, Options } from "../../../types";
/**
 * Matches arrays that contain all elements specified in the query.
 */
export declare const $all: (selector: string, value: Any, options: Options) => (_: import("../../../types").AnyObject) => boolean;
