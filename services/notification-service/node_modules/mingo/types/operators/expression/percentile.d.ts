import { Any, AnyObject, Options } from "../../types";
/**
 * Returns an array of scalar values that correspond to specified percentile values.
 */
export declare const $percentile: (obj: AnyObject, expr: Any, options: Options) => number[];
