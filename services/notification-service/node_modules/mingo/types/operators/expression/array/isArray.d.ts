import { Any, AnyObject, Options } from "../../../types";
/**
 * Determines if the operand is an array. Returns a boolean.
 */
export declare const $isArray: (obj: AnyObject, expr: Any, options: Options) => Any;
