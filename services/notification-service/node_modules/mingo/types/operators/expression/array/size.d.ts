import { Any, AnyObject, Options } from "../../../types";
/**
 * Counts and returns the total the number of items in an array.
 */
export declare const $size: (obj: AnyObject, expr: Any, options: Options) => Any;
