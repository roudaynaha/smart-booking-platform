import { Any, AnyObject, Options } from "../../../types";
/**
 * Returns true if two sets have the same elements.
 */
export declare const $setEquals: (obj: AnyObject, expr: Any[], options: Options) => Any;
