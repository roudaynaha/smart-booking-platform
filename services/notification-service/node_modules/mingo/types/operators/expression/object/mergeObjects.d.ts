import { Any, AnyObject, Options } from "../../../types";
/**
 * Combines multiple documents into a single document.
 */
export declare const $mergeObjects: (obj: AnyObject, expr: Any, options: Options) => AnyObject;
