import { Any, AnyObject, Options } from "../../../types";
/**
 * Returns the year for a date as a number (e.g. 2014).
 */
export declare const $year: (obj: AnyObject, expr: Any, options: Options) => number;
