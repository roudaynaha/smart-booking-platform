import { Any, AnyObject, Options } from "../../../types";
/** Returns the hyperbolic cosine of a value that is measured in radians. */
export declare const $cosh: (obj: AnyObject, expr: Any, options: Options) => Any;
