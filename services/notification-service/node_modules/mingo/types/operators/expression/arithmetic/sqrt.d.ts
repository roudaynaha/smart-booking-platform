import { Any, AnyObject, Options } from "../../../types";
/**
 * Calculates the square root of a positive number and returns the result as a double.
 */
export declare const $sqrt: (obj: AnyObject, expr: Any, options: Options) => number | null;
