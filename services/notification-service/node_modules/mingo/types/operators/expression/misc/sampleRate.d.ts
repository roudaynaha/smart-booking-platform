import { AnyObject, Options } from "../../../types";
/**
 * Randomly select documents at a given rate.
 */
export declare const $sampleRate: (obj: AnyObject, expr: number, options: Options) => boolean;
