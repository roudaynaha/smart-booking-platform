import { Iterator } from "../../lazy";
import { Options, SortSpec } from "../../types";
/**
 * Sorts all input documents and returns them to the pipeline in sorted order.
 *
 * See {@link https://www.mongodb.com/docs/manual/reference/operator/aggregation/sort/ usage}.
 */
export declare function $sort(coll: Iterator, sortKeys: SortSpec, options: Options): Iterator;
